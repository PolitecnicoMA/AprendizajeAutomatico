# Models

En este proyecto no se almacenaron modelos entrenados como archivos externos.

El algoritmo K-Means fue ejecutado directamente desde el notebook
`02_modelo_clustering.ipynb` y los resultados fueron analizados dentro del mismo entorno de trabajo.
